const data = {
  fronted: "Jose",
  backend: "Anibal",
  design: "SuTia",
};

const entries = Object.entries(data);
console.log(entries);

const values = Object.values(data);
console.log(values);
console.log(values.length);
